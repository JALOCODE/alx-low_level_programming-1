#include "main.h"

/**
* _isdigit - checks whether a characte is a digit or not
* @c: tested character
* Description: checks for a digit (0 through 9)
* Return: 1 if character is a digit 0 otherwise
*/

int isdigit(int c)
{
if ((c >= 48) && (c <= 57))
{
return (1);
}
}
