#include "main.h"

/**
* _isdigit - checks whether a characte is a digit or not
* @c: tested character
*/

int isdigit(int c)
{
        if ((c >= 48) && (c <= 57))
        {
                return (1);
        }
}
